# Copy to .env.local and fill in. Next.js loads .env.local automatically; the
# eval/run.ts tsx script reads it via its own lightweight loader.

# Google AI Studio API key — Gemini 2.5 Pro for draft, 2.5 Flash for verify.
GEMINI_API_KEY=

# Notion integration token (used in step 6).
NOTION_INTEGRATION_TOKEN=

# Notion parent page ID for "Relay Drafts" (used in step 6).
NOTION_DRAFTS_PAGE_ID=
